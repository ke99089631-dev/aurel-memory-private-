---
tags: [episodic]
type: log
date: 2026-09-07
---

# 2026-09-07

## FRED毎日0/3の真因を確定＝取得リクエストに `Accept` ヘッダが無かった（1行の欠け）。前2回の見立ては両方とも外し。

### 結論（1文）
FREDへの取得が `Accept` ヘッダを送っておらず、FRED(Akamai)が不完全リクエストと見なして**TLSは張るのに本文を返さず15秒でハング**させていた。1行足して解消。**ゼロ金・読取専用・可逆**。

### 実測で確定（read-only・機関ファイルは fetch部の1行のみ・バックアップ有り）
`~/.aurel/tmp/fetch_confirm.py` で 3系列×3ラウンド=9回を A/B:
| 条件 | 結果 |
|---|---|
| Accept ヘッダ**無し**（＝従来の器） | **9/9 FAIL**・毎回ちょうど15秒 TimeoutError |
| Accept: */* **有り** | **9/9 OK**・0.06〜0.52秒・209693/12718/31816 B 満額 |

補強の切り分け（同一IP 184.27.168.33・同一URL fredgraph.csv）:
- curl（http2/http1.1 どちらも）→ 200 が 0.13〜0.21s。urllib（Accept無し）→ 15s read timeout。差はTLS指紋かヘッダのみ。
- urllib に `Accept: */*` を足すと即成功 → **ヘッダ欠落が真因**（TLS指紋ではない）。IPv4限定でも同じ＝IPv6でもない。HTTP版でもない。

### 修正（適用済み・実証済み）
- `empire/circulation/external_macro.py` L86 `_fetch_latest`: headers に `"Accept": "*/*"` を追加（コメントで真因も明記）。
- バックアップ: `circulation/external_macro.py.bak_20260907_accepthdr`。
- **本番の器で実証**: `python -m circulation.macro_followup --force` → **3/3 RECOVERED**・`last_success 2026-09-06 → 2026-09-07`・`fetched_date=2026-09-07`。「実装した」でなく「本番が拾った」を満たす。
- 共通取得部なので **明朝07:00の循環も後追いを待たず直接取れるはず**。後追い4拍は真の間欠障害用の保険として残置。

### ★ 見立ての訂正（今回で通算2回訂正＝同じ穴を2回踏んだ）
- 9/4-9/5: 「07:00にネットが起きていない」＝**誤り**（他ホストは200即答）。
- 9/6: 「FRED側の気まぐれ throttling」＝**方向は合っていたが不完全**。ランダムではなく、**こちらのリクエスト形状（Accept欠落）に対する決定論的なタールピット**だった＝**こちら側で直せるものだった**。
- 多拍化（4拍）は症状への対処で、真因を叩けば1拍で足りる。無駄ではない（保険）が、本丸ではなかった。

### 教訓
- 「相手のせい（気まぐれ）」で止めると、こちらで直せる決定論的原因を見逃す。**A/B（有無を並べて各9回）を回すまで原因を断定しない。**
- curl は通るのに自コードは落ちる → プロトコル/IP/ヘッダを1つずつ潰す。今回はヘッダで即決着。
- 「毎回ちょうど同じ秒数(15s)で失敗」は**タイムアウト＝相手が黙る**の指紋。ランダムなら秒数がばらつく。

### 安全性（不変）
読取専用・鍵不要の公開CSV・金1円も動かさない・live/プロップ(g4_)非接触。書込は `external_macro.json`+`macro_followup.json` のみ。キルスイッチ2枚健在。器の変更は fetch部1行＋バックアップ、可逆。

---

## 【会長GO】「ゴムの件GOだ」 — stat_arb 共和分問題 起案①②③ 着工

### 一次情報（会長発言・原文）
> 「ゴムの件GOだ。」（2026-09-07・携帯フル稼働窓）

### AURELの解釈（＝この解釈で着工した。違えば訂正を請う）
「ゴム」＝会長への説明で使った比喩。**ペアトレード=「ゴムひもで結んだ2つのボール」／共和分=「本当にゴムひもが付いているか」**
（出典 feedback/LOG.md 日常語ルール#2）。よって「ゴムの件」＝ **2026-09-06 OPEN 案件
「stat_arb の組の選別が実データに基づいていない」の起案①②③** と解釈。

### GOの射程（安全境界の確認）
- 対象は **paper・金ゼロ・adoption 0・live_gate LOCKED**。実弾は一切含まない。
- ①②＝**測定**（読取専用・元々AURELの自律範囲）。GOにより優先度を上げて着工。
- ③＝**器(stat_arb.py)の改造**。これは「起案→会長GO」の対象。**本GOは着工の号令と受け取るが、
  実際にコードへ手を入れる前に『何をどう変えるか』の起案を提出し、そこで再度GOを取る。**
  理由＝9/6に自分で書いた留保（多重比較・半減期の乖離・構造変化）が未解消のまま器を触るのは、
  「測ったことにされている数字」を別の未検証数字で置き換えるだけになるため。

### 着工したもの（READ-ONLY・機関ファイル無改変）
1. `~/.aurel/tmp/coint_sweep.py` — 58銘柄 総当たり1653組。EG共和分＋**ベータ1のADF**（＝器が
   実際に張っている ln(A)-ln(B) そのものの定常性）＋半減期＋相関。**Benjamini-Hochberg FDR(q=0.10)**
   で多重比較を処理（1653検定なら p<0.05 は偶然でも約83組出る＝生pは結論に使わない）。
2. `~/.aurel/tmp/coint_validate.py` — 起案①。ローリング窓(750日/125歩)＋OOS(前70%で選別・後30%で
   のみ損益)＋**G4実測コスト差引**（片道0.272bps×2脚×回転、スパイク440pt=×2.75、ストレス×5）。
   損益の器は stat_arb.py と同一ロジックを写した（trailing W=20 / |z|>=1.5 / 先読み無し）。
   現行HELD3組を**対照群として同じ土俵に乗せる**＝起案③の判断材料になる。

### 着工時点で判明した追加の穴（sweep実行前の静的読みで確定）
**器は ln(A)-ln(B)＝ベータ1 の固定比率でしか張れない**（`stat_arb.py` L146）。
一方 9/6 の probe が測った共和分は **OLSベータ**。つまり「EG検定が通る」ことと
「この器で張れる」ことは別物。ベータが1から離れる組は、EGが通っても器は共和分でないものを張る。
→ sweep では **EG p と ベータ1ADF p の両方**を測り、両方FDRを通った組だけを生存とした。
（9/6の probe はこの区別をしていなかった＝私の測定側の不足。訂正して織り込み済み。）

## 【成績】共和分スイープ＋OOS検証 結果確定（2026-09-07 11:11・READ-ONLY・金ゼロ）

### 結論（1文）
**現行HELD3組に「ゴムひもが付いている」証拠は実データに無かった。** 58銘柄1653組を総当たりし、
FDR(q=0.10)＋ベータ1ADF＋I(1)を全部通ったのは**実質3組だけ**、うち2組はOOSで崩壊。器が張れる形で
生き残ったのは **WTI_XLRE 1組のみ（それも弱い）**。

### 対照群＝現行HELD3組（OOS＝後30%・G4実測コスト差引・typical想定）
| 組 | IS共和分 eg_p | ローリング窓で共和分だった割合 | OOS純益(bps) | OOS Sharpe |
|---|---|---|---|---|
| AUDUSD_NZDUSD | 0.449 | 3%(37窓中1) | **-719** | -0.41 |
| EURUSD_GBPUSD | 0.425 | 10%(42窓中4) | +1,378 | 0.51 |
| XAUUSD_XAGUSD | 0.414 | 9%(47窓中4) | +1,986 | 0.13 |
- **3組とも IS の共和分検定を通っていない**（p=0.41〜0.45／有意は<0.05）。
- ローリングで見ると共和分が成立していた窓は**3〜10%**＝ほぼ全期間「ゴムひもは付いていない」。
- EURUSD_GBPUSDのSharpe0.51はISもOOSも安定＝**共和分ではない別の何か（トレンド/キャリー）**で
  稼いでいる疑い。ベータOLS=0.44＝器が張るベータ1とは乖離。
- XAUUSD_XAGUSDはIS -6,201bpsの大負け→OOSだけプラス＝**期間依存**。Sharpe0.13は実質ノイズ。

### 総当たりスイープ（58銘柄・1653組）
- 検定済 1234組。**FDR(q=0.10)＋ベータ1ADF＋I(1) を全部通過＝5行**、
  うち4行は同一物の重複（SOL/SOLUSDT）→ **実質3組**: LTCUSDT_SOL / ADAUSDT_SOL / WTI_XLRE。
- **I(0)＝定常で共和分の相手にならない銘柄3つを除外**: BNBUSDT・NATGAS・VIX。
  （VIXを共和分の材料に使うのは原理的に誤り。器の候補リストに入っていたら外す。）

### 生存3組のOOS成績（同じ土俵）
| 組 | IS Sharpe | OOS純益(bps) | OOS Sharpe | 判定 |
|---|---|---|---|---|
| WTI_XLRE | -0.08 | **+3,205** | **+0.32** | 唯一の生存。ただし弱い・IS負け |
| LTCUSDT_SOL | +0.78 | -2,275 | -0.86 | **OOSで崩壊**＝IS過学習 |
| ADAUSDT_SOL | -0.24 | -3,933 | -1.39 | 却下 |
- 暗号2組は履歴1000日・ローリング窓が3つしか取れない＝**そもそも検定に耐える長さがない**。

### 罠を1つ検出（重要）
`GLD_XAUUSD` が **OOS Sharpe 4.44 / +14,438bps** と突出。だがこれは
**GLD＝金ETF、XAUUSD＝金スポット＝同一物**。共和分ではなく**同じ物の値付けズレ**で、
半減期14日・ベータ0.965がその指紋。実行速度とコストで消える種類の数字＝**採用してはならない**。
→ 器に「同一原資産の重複ペアを弾く」フィルタが無いことが判明（`near_duplicate`列で今回は手動排除）。

### 9/6の自分の手の訂正
`WTI_USDCAD`（9/6にEG p=0.015で「通った」と報告した組）→ **ベータOLS=-2.58**。器はベータ1しか
張れないので、**この組は器では張れない**。実際にベータ1で回すと IS -9,971 / OOS -6,607 bps の大負け。
＝**9/6の「共和分が通った」は器で実現できない数字だった**。多重比較(1653検定)を通すと生p<0.05は
偶然でも約83組出る＝**生pで通ったと言ってはいけない**を自分で踏んだ。

### 次の一手（起案③の判断材料が揃った）
1. **現行HELD3組は根拠なし** → 器の選別ロジック(`stat_arb.py`)を「実データのFDR＋ベータ1ADF＋I(1)＋
   同一物除外」に差し替える起案を提出（着工前に会長GOを取る＝9/7の約束どおり）。
2. 差し替え後に残る組は**多くて1〜2組**。「組が減る＝機関の稼働が下がる」ので、
   **組を増やすために銘柄ユニバースを広げる**か、**共和分以外の柱を立てる**かの分岐を会長に諮る。
3. **金は1円も動いていない・adoption 0・live_gate LOCKED・プロップ(g4_)非接触**は不変。


---

## 【継続セッション】会長「AからBで！」— 着工状況の引き継ぎ（12:03 携帯フル稼働窓）

### 一次情報（会長発言・原文）
> 「AからBで！」（2026-09-07 12:03）

### 解釈と確認（＝前窓の続き。コード内コメントに `会長GO「AからBで」` が既に刻まれている）
前の窓（コンテキスト満杯で切れた）で同じ号令を受け、**A→Bの順で着工済み**だった。
- **A＝器の選別を「手書きの数字」から「実データの測定」へ差し替える**（起案③）。
- **B＝銘柄ユニバースを広げて、本物の組を探す**（9/7 11:11 報告の分岐(2)の前半）。

### 引き継ぎ時点の実状態（本窓で実測確認・12:03〜）
| 物 | 時刻 | 状態 |
|---|---|---|
| `circulation/stat_arb.py` | 12:00 | **A完了**。手書き `coint_strength` を退役(`LEGACY_INSTRUMENTS`＝検算用の死蔵)、組の定義は `arb_screen.json` から読む。選別ファイル欠落時は**手書きへ黙って戻らず0組**（再発防止）。selftest も銘柄名決め打ちを廃し「性質」を検証する形へ書き換え済。バックアップ `stat_arb.py.bak_20260907_measured_selection` |
| `circulation/arb_screen.py` | 12:02 | **新規・A の測定器**。関門= G1定常脚除外 / G3同一物除外 / G4ベータ帯 / G6期間外 / G7持続性 / FDR。`--selftest` **PASS**（本窓で実走確認・「関門が実際に落とす」ことを検証） |
| `data/arb_universe/` | 11:58 | **B の拡張棚**。新規 96 本の日足CSV（既存58本とは別棚・同名は既存優先＝既存の数字を上書きしない） |
| `data/circulation/arb_screen.json` | 11:54 | **拡張前(58銘柄)の結果＝survivors 0**。拡張棚を入れた再走はまだ |

### 本窓でやったこと
1. venv 特定（`C:\Users\user\ImperialFlow\venv\Scripts\python.exe`＝循環と同じ器。素の python は numpy 不在で落ちる）。
2. `arb_screen --selftest` → **PASS**。
3. **拡張棚込みの本走を起動**（≈150銘柄・約1万組）。ログ= `~/.aurel/tmp/arb_screen_full_expanded.log`。

### 安全性（不変）
paper のみ・**金ゼロ**・adoption 0・live_gate LOCKED・プロップ(g4_)非接触。書込みは
`arb_screen.json` と `stat_arb*.json`（裁定の紙帳簿）のみ。器の改変はバックアップ有り＝可逆。

## 【結果確定】B＝ユニバース拡張の総当たり 完了（2026-09-07 17:15・READ-ONLY・金ゼロ）

### 結論（1文）
**銘柄を58→154本へ広げ、1万組を総当たりしても、器で張れる「本物のゴムひも」は1組も無かった。**

### 数字
- 銘柄 154本 / うち I(1)＝共和分の材料になる脚 149本（除外5本＝BNBUSDT・FXF・IEI・NATGAS・VIX＝それ自体が定常）。
- 検定した組 **10,053**。FDR(q=0.10)を通ったのは **3組だけ**（閾値 eg=0.00027 / b1=0.00004）。
- その3組は **BTC_BTCUSDT / ETH_ETHUSDT / SOL_SOLUSDT** ＝**全部が同一物の別表記**。G3(水準相関>0.995)で正しく排除。
- **【採用】0組**。所要486秒（前回までの途中経過を再利用したぶんを除く）。

### 対照群＝現行HELD3組＋旧手書き2組（全部落ちた・理由つき）
| 組 | 落ちた理由 |
|---|---|
| AUDUSD_NZDUSD | FDR不通過 / 期間外 純益 -719bps / 繋がっていた窓 16%<20% |
| EURUSD_GBPUSD | **器(ベータ1)では張れない beta=0.442** / FDR不通過 |
| XAGUSD_XAUUSD | FDR不通過 / 期間外シャープ 0.13<0.25 / 繋がっていた窓 9%<20% |
| BTC_ETH | FDR不通過 / 期間外 純益 -2,531bps / 繋がっていた窓 10%<20% |
| USDCAD_USDNOK | **器(ベータ1)では張れない beta=0.629** / FDR不通過 |

### 配線の実見（＝「実装した」でなく「器がそう振る舞う」を確認）
- `stat_arb --selftest` **PASS**。`selection: arb_screen.json (measured) 生存=0 不採用=8組(理由つき)`。
  手書き混入なし・ノブ単調・選別ファイル欠落時は0組（黙って手書きへ戻らない）・先読み無し(point-in-time=True)。
- `stat_arb` 本走 → **held=0/0・total_pnl=0.0**。裁定兵は**何も張っていない**状態で公開面に出た。
- ★これは「壊れた」のではなく**「根拠が無いので張らない」を器が正直に表明している**状態。
  9/6以前は根拠ゼロの3組を張り続けていた＝**嘘の稼働が消えた**のが今回の実利。

### 途中経過の保存（本日の副産物・器の改良）
1万組の計算は十数分かかるが、対話の窓が閉じるとプロセスが道連れで落ちる（**同じ全滅を3回**）。
→ `arb_screen.py` に **再開可能な途中経過**（`data/circulation/arb_screen_cache.json`・単一書き手＝arb_screen）を実装。
   - 各組に「両脚のバー本数＋最終日」の指紋を付けて保存。**バーが1本でも増えたらその組だけ自動で測り直す**＝古い数字が残らない。
   - つまみ署名（MIN_OVERLAP/maxlag/REFINE_P/FDR_Q/z_entry/W）が変われば全部捨てる。
   - 書込みは一時ファイル→`os.replace`＝途中で落ちても壊れた途中経過を残さない。
   - 効果の実見: 再開時 `1段目: 11026/11026 組は前回の結果を再利用（残り0組だけ測った）`。
   - **測定結果は一切変わらない**（同じ入力→同じ数字）。selftest PASS で確認。

### 安全性（不変）
paper のみ・**金ゼロ**・adoption 0・live_gate LOCKED・プロップ(g4_)非接触。読取は bars_*.csv のみ。
書込みは `arb_screen.json` / `arb_screen_cache.json` / `stat_arb.json`（裁定の紙の公開面）だけ。
バックアップ: `stat_arb.py.bak_20260907_measured_selection` / `arb_screen.json.bak_core58_20260907_1207`。

### 会長へ差し戻す判断（AURELは採否を決めない）
1. **このまま0組で置く**（裁定兵は休止＝嘘をつかない状態を維持）。
2. **関門を緩める**（例: 期間外シャープの下限0.25や、繋がっていた窓20%を下げる）。★ただし
   これは「基準を結果に合わせて動かす」行為＝G4.5で会長が固めた原則に反するので、AUREL推奨せず。
3. **共和分以外の柱を立てる**（裁定を諦め、別の稼ぎ方＝方向・キャリー・ボラ売り等へ研究予算を回す）。
AUREL推奨＝**1と3の併走**（休止させたまま、次の柱の起案を作る）。

## 【会長GO】「君の推しでいこう」＝(1)裁定は0組で休止 ＋ (3)次の柱の起案 — 2026-09-07 17:30

### 一次情報
> 「君の推しでいこう。」（AURELの推し＝**1＋3の併走**＝裁定は0組のまま休ませ、共和分以外の次の柱の起案を作る）

### 起案を書く前に確認したこと（READ-ONLY・5分・金ゼロ）
「次の柱を建てよう」と書き出す前に、**既存の柱が stat_arb と同じ病気でないか**を先に確認した。
理由＝土台が同じ病気なら、その上に新しい柱を建てても同じ嘘が増えるだけだから。

### ★結果：**同じ病気だった。9兵すべて、組の選別は手書きの数字。**
| 兵 | 選別に使っている数字 | それは測ったものか | 損益の器 |
|---|---|---|---|
| trend_follow | `trend_strength` 0.85/0.78/0.70/0.58/0.50 | **手書き** | 実バー(SPY/GLD/USDJPY/EURUSD/GBPUSD) |
| mean_reversion | `typical_z_reach` 2.6/2.3/2.1/1.8/1.6 | **手書き** | 実バー(クロスは実メジャーから実算出) |
| carry | `carry_bps_day` / `vol_bps_day` | **手書き**（実スワップ金利を取っていない） | 実バー |
| stat_arb | `coint_strength` | **手書き→本日実測へ差替済（＝0組）** | 実バー |
| vol_sell | `iv_rank` 0.80〜0.40 | **手書き**（オプションのIVデータが機体に無い） | partial |
| tail_hedge | `crisis_prob` 0.016〜0.009 | **手書き** | partial(原資産で代理) |
| event_driven | `hit_rate` 0.72〜0.45 | **手書き**（イベントの実結果を持っていない） | **False** |

＝**stat_arb は例外ではなく、最初に開けた1個目の箱だっただけ**。
「損益は実データ／選別は手書き」という**二層構造が機関の標準形**になっていた。

### 起案（＝これが「次の柱」の答え。着工＝会長GO待ち・測定は自律で先行可）
**新しい柱を建てる前に、既に建っている柱が本物かを同じ物差しで測る。** 優先順は「測れる順」。
1. **[今すぐ測れる] trend_follow / mean_reversion** — 損益の器が既に実バーで動いており、本日Bで
   **154銘柄の棚**が手元にできた。同じ関門（前70%で選別・後30%は選別に使わない／G4実測コスト差引／
   多重比較FDR／ローリング窓での持続性）を当てれば、手書きの強度を実測へ置き換えられる。
   ★**Bの96銘柄は無駄にならない**。裁定では実らなかったが、この測定の材料そのもの。
2. **[要データ] carry** — 実スワップ/ファンディング金利が無いと測れない。まずデータの入手可否を調べる。
3. **[原理的に測れない] vol_sell / tail_hedge / event_driven** — オプションのIV・イベントの実結果を
   機体が持っていない。**測れないものを「動いている」と表示し続けるのが一番危ない**ので、
   測定の可否がはっきりするまでは「根拠なし」と正直に表示する扱いを提案する（stat_arbと同じ作法）。

### AURELの見立て（正直に）
今日の収穫は「裁定が空振りだった」ことではなく、**機関の稼働の大半が未検証だと分かったこと**。
数字が減るのは痛いが、**減ったのは嘘の数字**であって実体ではない。金は最初から1円も動いていない。

### 安全性（不変）
本セクションは読取のみ・機関ファイル無改変・金ゼロ・adoption 0・live_gate LOCKED・プロップ(g4_)非接触。
