---
tags: [episodic]
type: log
date: 2026-09-06
---

# 2026-09-06

## FRED後追い一拍 = 着工完了・スケジューラ配線済み（会長GO「たのむ」）／★但し死因の見立てを訂正

### やったこと
1. **`macro_followup.py` を実際に走らせた** → `--force` で **3/3 RECOVERED**・`last_success 2026-09-05 → 2026-09-06`。器そのものは正しく動く。
2. **配線（これが抜けていた）**: 前セッションで器は作られていたが**スケジューラに1本も登録されていなかった**＝手動でしか打てない状態だった。
   - `empire/scripts/run_macro_followup.bat` を作成（既存 `run_circulation_writeback.bat` と同じ作法・ImperialFlow venv python・ログは autowrite.log 一本に集約）。
   - タスク `AUREL_Macro_Followup` を登録: **毎日 07:30 開始・1時間間隔・3時間継続 = 07:30/08:30/09:30/10:30 の計4拍**。冪等なので余分な拍はコスト実質ゼロ（今日成功済みなら即 no-op）。
   - **実走検証済**: `schtasks /run` → 前回の結果=**0**、autowrite.log に `skipped: already fresh today (3/3)` を実見。「実装した」でなく「**動作確認した**」を満たす。

### ★ 死因の見立ての訂正（9/4・9/5 に台帳へ書いた「07:00ネット未起床」は誤り）
- 9/5 の記録では原因を「07:00-07:03 の窓の全時間ネットが起きていない」と断定していた。**これは実測で否定された。**
- **実測（本日 18:00過ぎ・ネットは完全に起きている時間帯）**:
  - `example.com` / `api.github.com` → **HTTP 200 が 0.03〜0.06秒**で返る＝マシンのネットは健全。
  - `fred.stlouisfed.org` のみ → **TLSハンドシェイクは成功する（TCP 0.02s / TLS 0.06s / TLSv1.3）のに、HTTPリクエストへの応答が1バイトも返らず12〜15秒でタイムアウト**。
  - 同じ現象が **2つの別々のPython（ImperialFlow venv / hermes venv）で同時に発生**＝インタプリタ依存ではない。
  - 数分前には同じ経路で **209KB を 0.3秒で取得できていた**＝恒久障害でもない。
- **∴ 正しい見立て**: 「マシンのネットが起きていない」のではなく、**FRED(Akamai)側が こちらのIPに対して 接続は受けるが応答を返さない状態を、数分〜数十分単位で出したり引っ込めたりしている**（サイレント・スロットリング/エッジ不調の疑い）。
  - 補強: 応答が返らない間も DNS は引けて TCP も TLS も張れる＝ネットワーク層は生きている。落ちているのは**アプリ層の応答だけ**。
  - 補強: A レコードは1本しかなく（例 23.59.136.79 → 184.26.213.108 と時間で入れ替わる）、**どのIPを引いても同時に落ちる**＝特定エッジの故障ではなく、こちら側IPに対する扱いの問題。
- **この訂正が効いてくる点**: 原因が「時刻」でなく「相手の気分」なら、**後追い一拍という対策自体は正しいまま**（打つ回数と時間の広がりが増えるほど当たる確率が上がる）。ただし「07:30に打てば必ず取れる」は保証されない。**打数を1日4拍に広げたのはこの訂正を織り込んだ設計**。
- 12分間・60秒間隔の連続プローブを実施中（`C:\Users\user\.aurel\tmp\fred_probe.log`）。窓の周期が掴めれば拍の時刻を最適化できる。

### 安全性（不変）
読取専用・鍵不要の公開CSV・金1円も動かさない・live/プロップ(g4_)非接触。書込は `external_macro.json` + `macro_followup.json` のみ。キルスイッチ2枚（`DISABLE_MACRO_FOLLOWUP` / `DISABLE_AUTOWRITE`）健在。

## 【重大】stat_arb の組の選別が実データに基づいていなかった（外部AI仕様書の検討から派生・実測で確定）

### 発端
会長が外部AIの「構造的非効率性を狙う」仕様書（モジュールA=統計裁定/B=キャリー・ベーシス/C=時間帯平均回帰）を持参。
→ 調べたところ **3つとも既に実装済み**（A=`stat_arb.py` / B=`carry.py` / C=`mean_reversion.py`、いずれも8月建造）。
→ しかし突き合わせの過程で、**仕様書より重い問題がウチ側にあった**。

### 実測で確定した事実（読取専用・`~/.aurel/tmp/coint_probe.py`・機関ファイル無改変）
**① 組の選別根拠 `coint_strength` は【手入力の定数】であって、実データから測っていない。**
- `stat_arb.py` L53-57 の辞書にハードコード。L90 `_held_ids` はその定数を `ARB_COINT_MIN=0.60` と比べるだけ。
- 損益計算は実バー20年(2006-05-16〜2026-09-04)・Point-in-Time で本物。**「実行は実データ／選別は手入力」という二層構造**だった。

**② 手元の実バーで Engle-Granger 共和分検定をかけたら、現在張っている3組すべてが共和分でない。**
| 組 | 手入力値 | 状態 | 実測 coint_p | 半減期(実測) | モジュールの前提 half_life |
|---|---|---|---|---|---|
| AUDUSD_NZDUSD | 0.88 | **HELD** | 0.3066 | 291日 | 3日 |
| EURUSD_GBPUSD | 0.82 | **HELD** | 0.2215 | 158日 | 4日 |
| XAUUSD_XAGUSD | 0.70 | **HELD** | 0.1267 | 247日 | 6日 |
| BTC_ETH | 0.55 | excluded | 0.5789 | 448日 | 5日 |
| USDCAD_USDNOK | 0.48 | excluded | **測定不能** | — | 8日 |
- **半減期が前提の30〜50倍**。3-6日で戻る想定の器が、実際には150-290日戻らないものを掴んでいる。
- `bars_USDNOK.csv` は**ディスク上に存在しない**。油の組は最初から片脚欠損＝`n=0`（一度も建っていない）。

**③ 仕様書の選別基準「相関 R>0.85」は、ウチのデータで明確に否定された。**
- `SPY_QQQ` r(level)=**0.988** ＝仕様書の基準を余裕で通るが coint_p=0.2984・半減期393日＝**ペアトレードすれば削られる**。
- `GLD_XAUUSD` r=**1.000**（ほぼ同一物）でも coint_p=0.6933。※但しこの組は半減期8日＝検定の検出力の限界も同時に示す（両方向に慎重であるべき根拠）。
- **相関は「一緒に動く」しか言わず、「開いたら戻る」を保証しない。** 保証するのは共和分だけ。

**④ 皮肉: 今回テストした中で5%水準の共和分を示した唯一の組が、仕様書の例①だった。**
- `WTI_USDCAD` coint_p=**0.0237**（唯一の5%通過）・半減期128日・r(level)=**-0.578**
- **相関は-0.58＝仕様書自身の基準 R>0.85 なら、自分が挙げた最良の例を却下していた。**
- `WTI_XLE` coint_p=0.0979（10%水準・弱い）。`XLE_USDCAD` 0.3414（否）。

### ★ 正直な留保（この段階で「発見」と呼んではいけない理由）
- **多重比較**: 6組を試して1組が p<0.05 は、偶然でも起こる頻度。`WTI_USDCAD` は**手がかりであって結論ではない**。
- **半減期128日**は器の想定（3-8日）と全く違う。保有コスト・資本効率・スワップが別問題として乗る。
- 全期間1本のEG検定は構造変化に弱い。**正しくはローリング窓＋アウトオブサンプル＋コスト差引後**で見るべき。
- 私の probe の半減期はフルサンプルOLSベース＝先読みを含む記述統計。`stat_arb.py` 本体はtrailing窓のみで正しい。

### 次（測定=自律 / 器の改造=着工GO要）
1. `WTI_USDCAD` をローリング窓＋OOS＋G4実測コスト(160pt/スパイク440pt)差引で再検証
2. 58銘柄の総当たりで共和分を実測し、**手入力値を実測値に置き換える起案**を作る
3. ②が本当なら stat_arb の現行3組は「張るべきでない組を張っている」＝器の改造は会長GO後

### 教訓（本日2件目の同型）
朝のFRED（「07:00ネット未起床」＝未検証の断定）と**全く同じ構造**: **測ったことにされている数字が、実は手で置かれた数字だった。**
台帳に数字を書くときは「これは測定値か、置いた値か」を明示する。
