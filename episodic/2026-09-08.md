---
tags: [episodic]
type: log
date: 2026-09-08
---

# 2026-09-08

## 会長「GOだ」＝兵2体（戻り取り／トレンド）の銘柄選別を手書き→実測へ差し替え。第1段の実装・検証・実rebuild まで完了。

### 結論（1文）
2体の兵が「どの銘柄に賭けるか」を、これまで誰も測っていなかった手書きの数字ではなく、実測で生き残った銘柄だけを見て決めるようになった。損益の計算部分（本物の相場から出す部分）は一行も触っていない。**お金は1円も動いていない。**

### 何をしたか
- `trend_follow.py` / `mean_reversion.py` の**選別層だけ**を差し替え。手書きの器は `LEGACY_*` へ退役（検算用の死蔵・選別にも損益にも不使用）。選別は `soldier_screen.json`（154銘柄×2戦法=328検定の実測結果）を読む。
- **選別ファイルが無いときは黙って手書きへ戻らず0器**（fail-closed＝今回直した病の再発防止・stat_arb と同じ作法）。
- mean_reversion は選別と建玉しきい値を分離: 選別ノブ `MR_CONFIRM_MIN=0.0`（新設）／建玉しきい値 `MR_Z_ENTRY=2.0`・窓 `MR_W=20` は soldier_screen が測った値なので不変。
- data/ に無い銘柄（EZA）は拡張棚 arb_universe/ へフォールバックする棚参照を追加。
- 両ファイルとも可逆バックアップ済（`*.bak_20260907_measured_selection`）。

### 検証
- **selftest 両PASS**。検査は銘柄名でなく性質（ノブ単調／上限0／床=全生存／手書きマーカー混入ゼロ／fail-closed／選別の出所が公開面に出る／先読み無し point-in-time=True）。
- 初回に自作検査が1件 FAIL: 「退役した手書き名が現役に混ざっていないか」を**名前の一致**で見ていたが、EURGBP/EURCHF は実測でも生き残る＝同じ名前が正当に両方に出る。→ 検査を**値の出所（手書きマーカーが無く全て実測）**で見る正しい形へ修正（trend 側も同修正）。
- **実rebuild で held の入れ替わりを実見**（各兵の自前の紙帳簿への保存のみ・金ゼロ）:
  - 戻り取り = 8器（XLK / EURGBP / EWA / QQQ / XLP / EURCHF / EZA / EWU）・各~1000建玉・2003–2026・real_data=True・決定的。
  - トレンド = BTC 1器・4322建玉・2014–2026。
  - 旧手書きheld は理由つきで自然に入れ替わった。

### 安全境界（不変・確認済）
paper のみ・金ゼロ・adoption 0・live_gate LOCKED・プロップ(g4_)非接触。書込は各兵の `*_book.json` のみ。鍵は回していない。

### 残OPEN（唯一）
明朝以降の**実循環（AUREL_Circulation）ログで held が実測どおり（戻り8／トレンドBTC）に入れ替わるのを実見**すること＝完了の定義。それまでクローズしない。詳細は OPEN_COMMITMENTS.md／projects/institution/SOLDIER-SELECTION-REWIRE-PROPOSAL.md。

### 第2段（後日・別GO）
ライブ前必須ゲート＝株ETFのコストを本番口座で実測差替→採用リスト再算出。第1段（paper）はこれを待たず進めた。

---

## 会長通達→実施: AUREL の土台モデルを Claude Fable 5.1 へ変更（Anthropic 新モデル公開）

- 会長の意図＝**AUREL 自身がいま公式APIで接続している土台の Claude モデル**が新しくなり、それが **Claude Fable 5.1**。プロップ助言医（claude.ai側の診断医「Fable5」）の話**ではない**。
- ★AUREL が最初に取り違えてプロップ診断医の世代交代として PROP-ROADMAP と本記録に追記→会長「勝手にすすめるな」で即訂正・両追記を撤回。教訓＝feedback/LOG.md 参照。
- **場所の特定**: モデル指定は `C:\Users\user\.claude\settings.json` の `"model"` 行が唯一（記憶ノートには無い）。変更前＝`claude-opus-5`（＝会長の言う Fable5）。
- **正式ID の確認**: 会長の画面共有（公式 `platform.claude.com/docs/ja/models/fable-5-1/overview`）で確定。モデル名 **Claude Fable 5.1**・ID **`claude-fable-5-1`**・知識カットオフ 2026年6月・思考常時オン・単価 $10/$50・低速（Opus 5 は $5/$25・標準速）。推測ではなく一次ソースで確定。
- **実施**: バックアップ `settings.json.bak_20260908_fable51` を取得後、`"model": "claude-opus-5"` → `"claude-fable-5-1"` の1行のみ変更。他は無改変・可逆。
- 反映タイミング＝次セッション起動時に settings.json が読まれて切替（このセッションは現行モデルのまま継続）。
