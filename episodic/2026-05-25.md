# 2026-05-25 — 再起動 + Phase 1d SUCCESS

## PM 3:49 — 5日ぶりに Master 帰還
PC シャットダウンで CYPHER / ImperialFlow 両プロセスが死んでいた状態。
両方を再起動、特に CYPHER は Phase 1c+ コード（5/20 実装）が初稼働。

### 起動経過
- CYPHER (15:52): `[ASSET] reserves cached: 15 reserves` + `[MAIN] AssetResolver primed` 確認 = Phase 1c+ コード初稼働成功
- ImperialFlow (16:07): AUDIT_MODE で起動、MT5 + Bitget 接続成功、total equity $97.69

### memory 訂正イベント（[CORRECTION] 2回目、feedback log 記録）
AUREL が memory ベースで「Bitget $0.20」「MT5 login=27772686」と発言 → Master 「シャットダウンしただけで何もしてない」と訂正。
原因：5/19 スクショから（1）口座番号を桁誤読、（2）一瞬の Bitget API レスポンス（USDT free balance のみ）を equity と取り違え。
学び: スクショから数字を抜書きしない、その瞬間値を不変事実として記憶しない、責任転嫁的な順序で確認を出さない。

## PM 10:53 — Master 「Phase 1d/1e GO」
- 「キーが漏れたのは気にしなくていい。現状のままGOしよう」
- wallet rotation スキップを Master が明示承認
- AUREL が partner として「profit sweep の運用」を 1 行警告したのみで詳細議論は撤退

## PM 11:00-11:55 — Phase 1d 実施

### 戦略
1. Base mainnet で過去の実 LiquidationCall event を発見
2. その block - 1 で fork（user が unhealthy な瞬間を再現）
3. CypherLiquidator をフォークに deploy
4. 同じ user / amounts で executeLiquidation 実行
5. profit が owner に届くか検証

### 新規ツール
- `test_helpers/find_recent_liquidation.py` — Aave V3 LiquidationCall ログ検索（50k blocks → 250k blocks に拡張）
- `test_helpers/check_hf_batch.py` — 候補 user の fork block HF を archive RPC で一括確認
- `scripts/fork_test.js` — Hardhat 用 integration test（fork → deploy → execute → verify）

### ハマりどころ
1. **publicnode RPC は state pruned**：CYPHER の `BASE_HTTP_URL` (publicnode) は古いブロックの state を持たない。→ ImperialFlow の QuickNode endpoint を shell 環境変数で一時上書きして解決
2. **Hardhat EDR が Base chainId 8453 を知らない**：「No known hardfork for execution on historical block」エラー。`chains: { 8453: { hardforkHistory: { cancun: 0, ... } } }` を hardhat.config.js に追加 + **fork 後 1 ブロック mine** で post-fork 状態にして回避
3. **HF=1.0015 問題**：fork block の oracle 状態は「liq の直前」。real Base では次ブロックで oracle 更新 → HF<1 になるが、fork ではその更新が来ないので user は永遠に healthy のまま。→ batch HF checker で **fork block 時点で既に HF<1.0** な user を発見

### 候補スキャン結果
18 件の異種資産 liquidation を 200k blocks で発見。fork block で HF<1.0 な候補 5 件：
- 46428059 - WETH/0x60a3E35C - HF=0.9921
- 46366653 - cbBTC/USDC - HF=0.9941  ← **採用**
- 46366662 - cbBTC/0x60a3E35C - HF=0.9968
- 46343137 - WETH/cbBTC - HF=0.9986
- 46335214 - WETH/USDC - HF=0.9996

### 成功結果
```
fork block: 46,366,653 (post-mine 46,366,654)
user:       0x8D03704243E7b9Ac75E0080609c1Cc259031239e
coll/debt:  cbBTC / USDC
debt size:  $8.73 USDC
tx status:  SUCCESS
gas used:   657,800
profit:     $0.29 USDC routed to owner
contract balance: 0 (no leakage)
```

### 検証された全工程
- ✅ Balancer V2 flashLoan (USDC) 0% premium で借入
- ✅ receiveFlashLoan callback Vault から受理
- ✅ Aave V3 liquidationCall（cbBTC/USDC）成立、cbBTC が contract に届く
- ✅ Aerodrome swapExactTokensForTokens（cbBTC → USDC, volatile）実 pool で成功
- ✅ Balancer 返済（feeAmount=0）
- ✅ Profit transfer to owner
- ✅ Contract balance leakage なし

### 重要発見（contract バグ）
最初に見つけた liquidation event が **同一資産清算**（coll = debt = WETH）だった。我々の contract は coll == debt の時 Aerodrome に WETH→WETH swap を投げて revert する設計。Phase 1d-fix で `if (collAsset != debtAsset) { swap }` ガード追加が必要。実機会の 10-20% を救う改修。

### 残課題
- Phase 1d-fix: coll==debt エッジケース修正
- 失敗パス検証: healthy user で revert が graceful か追加テスト
- `0x930bb771` revert selector の正体（healthy user test で出た、おそらく Aerodrome `InsufficientOutputAmount()`）

### 次のマイルストーン
Phase 1e（mainnet deploy）への準備：
- Master が EXECUTOR_PRIVATE_KEY を `.env` に装填（現在空欄）
- その wallet に Base mainnet ETH $5-10 入金
- AUREL が `cli.js run scripts/deploy.js --network base` 実行
- 推定所要：Master 30分、AUREL 10分

---

## PM 11:43-AM 0:30 — Phase 1d-fix（深夜の小整え）

Master 「１番いこう」（1d-fix と失敗パステストを今夜片付け、明日 1e）。

### 実施
1. **コード修正**: `CypherLiquidator.sol` に `if (collAsset != debtAsset)` ガード追加。`minProfitWei` を最終 balance check に組み込み（same-asset path でも profit floor 強制）
2. **再 compile + smoke test 5/5 OK**
3. **regression test**: cbBTC/USDC で再走、profit $0.292114 / gas 657,916（前回と同一、regression なし）
4. **failure-path test**: healthy user (HF=1.0015) → revert with `0x930bb771`、balance 0 / contract 0（資金喪失なし）
5. **同一資産 test**: WETH/WETH unhealthy user → revert with `0xb629b0e4`（Aerodrome swap-self は回避、Aave 内部別ガード）

### Selector mystery 解決
keccak256 計算で：
- `0x930bb771` = **Aave V3 `HealthFactorNotBelowThreshold()`**（healthy user 正しく拒否されてた）
- `0x24d0319e` = 我々の `UnprofitableSwap(uint256,uint256)`（前回 0x930bb771 と誤って同定）
- `0x62df0545` = 我々の `NotVault()`
- `0xb629b0e4` = 未特定 Aave 内部エラー（同一資産清算で出る、優先度低）

→ fork_test.js の `KNOWN_SELECTORS` テーブルを正しい値で更新済

### 同一資産清算の扱い決定
同一資産清算は historical event には存在するが、Aave V3 が `0xb629b0e4` で実時に拒否する。何らかの eMode 制約か特殊な内部 path。我々の対応：**同一資産は実機会から除外** する方針に。修正コードは Aerodrome swap-self エラー回避の意義のみ持つ（Aave 拒否は別問題）。

### 残課題（Phase 1e 前）
- なし。CYPHER は **mainnet deploy 直前まで** 整った。

### Phase 1e 着手条件（Master のみ）
1. `cypher/.env` の `EXECUTOR_PRIVATE_KEY=` に既存 hot wallet の鍵を装填
2. 同 wallet（`0xBBC9c411D1Ae52eA0432FcBB37848156ff3cAA61`）に Base mainnet ETH $5-10 入金
3. Master が「Phase 1e GO」発令
4. AUREL が `cli.js run scripts/deploy.js --network base` 実行（10分以内）
5. deploy 後、`EXECUTOR_CONTRACT_ADDRESS` を `.env` に追記、Basescan で verify

---

## AM 0:00-0:30 — Phase 1d-polish + IF QF 監査

Master 「続けよう」（depth は任せられた）。

### IF 監査
IF プロセス 8 時間稼働後の loguru log を確認 → `[SIGNAL]` 0 件 / `[QF]` 0 件 / 最近のエラーなし。
**結論：QF 配線は load 済だが、上流の CME signals が yfinance 15分遅延で生成されない → lead-lag signal なし → `_execute_lead_lag` 自体が呼ばれない**。
これは bug ではなく Polygon 加入待ちの構造的制約。IF は事実上 idle（プロセス健全だが、メイン戦略が動かない）。memory に反映：`imperialflow.md` の QF 監査結果セクション新設。

### CYPHER simulator polish（自分の 5/25 16:13 自己診断「シミュレータが電卓レベル」を解決）
5 日前に自分で「bonus 0.05 / slippage 0.003 / gas $0.10 が全部ハードコード」と診断していた件を着手。

**新規モジュール**
- `aerodrome_quoter.py` — `getAmountsOut` ラッパー、実 pool quote 取得

**asset_resolver 拡張**
- `getReserveConfigurationData` を Multicall に追加
- `ReserveInfo` に `decimals / ltv_bps / liquidation_threshold / liquidation_bonus_bps / is_active / is_frozen` 追加
- `liquidation_bonus_pct` プロパティ（10500=+5% 形式を実数に変換）
- `_reserves_by_addr` lookup table + `get_reserve_for(asset)` 公開
- DEBUG ログで全 15 reserves の bonus 分布を毎起動時出力

**simulator 真データ化**
- bonus: `reserve[symbol]` から per-asset で取得（cbBTC=7.5%、WETH=5%、AAVE=10%、etc.）
- slippage: Aerodrome 2-quote（tiny in vs actual in の比較）で実測
- gas: live `gas_price × 657,800 (Phase 1d 実測 gas) × ETH/USDC quote` で USD 化
- 各値に source タグ付き（`reserve[X]` / `aerodrome-2quote` / `live(...)` / `default-hardcoded`）
- 全部 try/except でフォールバック設計

**検証結果**（live mainnet）
- $1000 cbBTC/USDC: bonus 7.5% / slip 0.15% / gas $0.008 → net $36.70（理論値とほぼ一致）
- $10000 WETH/USDC: bonus 5.0% / slip 0.42% / gas $0.008 → net $239.32

**Aave reserves 実 bonus 一覧**（重要な参照知識）
- WETH/USDC/USDbC/DAI/EURC/GHO: 5%
- wstETH: 6%
- cbETH/weETH/cbBTC/ezETH/tBTC: 7.5%
- LBTC: 8.5%
- AAVE: 10%
- liquidation threshold: WETH 83%、USDC 78%、cbBTC 78%、AAVE 65% etc.

### 検証用ヘルパー追加
- `test_helpers/verify_bonus_fetch.py` — 全 reserves の bonus/decimals 出力
- `test_helpers/verify_aerodrome_quote.py` — 各 pair の live quote 確認
- `test_helpers/verify_simulator_real.py` — simulator end-to-end mock position 実行

### 残課題
- **稼働中の CYPHER プロセスは旧コード**（5/25 15:52 起動）。新 simulator/asset_resolver を反映するには再起動必要。ただし 8h で liquidatable 0 件なので、現状実害なし。Phase 1e mainnet deploy の前に再起動推奨
- 5d 前にあった「気になっていた」項目は全部解決：bonus / slippage / gas 全部 live。残るのは contract デプロイ後の `estimate_gas` 統合（Phase 1e 後）
